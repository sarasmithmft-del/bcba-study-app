/**
 * Must equal full-length mock exam item count (official BCBA exam length).
 * Home page imports only this file (not full banks) so the landing route stays light.
 */
export const BCBA_MOCK_EXAM_EXPECTED_ITEMS = 185;
