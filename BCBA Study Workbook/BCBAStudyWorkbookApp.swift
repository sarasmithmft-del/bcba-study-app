import SwiftUI

/// SwiftUI entry point for BCBA Study Workbook.
///
/// In Xcode: File → New → Project → App → Interface: SwiftUI
/// Replace the generated App file with this one (or set this as @main).
/// Bundle Identifier: com.euphoria.bcbaworkbook
@main
struct BCBAStudyWorkbookApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
